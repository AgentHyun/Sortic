import {atom} from "jotai";

export const wholesaleLinksAtom = atom([]);
export const wholesaleCodesAtom = atom([]);
export const selectedLinkIdAtom = atom(0);
export const wholesaleCommissionAtom = atom(0);
