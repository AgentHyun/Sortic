<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.sortic.sorticproject.Mapper.UserMapper">
    
    <!-- 사용자 정보 저장 -->
    <insert id="save" parameterType="org.sortic.sorticproject.Entity.User">
        INSERT INTO users (username, password, email, created_at)
        VALUES (#{username}, #{password}, #{email}, #{createdAt})
    </insert>

    <!-- 사용자명으로 사용자 정보 조회 -->
    <select id="findByUsername" parameterType="String" resultType="org.sortic.sorticproject.Entity.User">
        SELECT id, username, password, email, created_at as createdAt
        FROM users
        WHERE username = #{username}
    </select>

    <!-- 사용자명 존재 여부 확인 -->
    <select id="existsByUsername" parameterType="String" resultType="boolean">
        SELECT EXISTS(
            SELECT 1 FROM users WHERE username = #{username}
        )
    </select>
</mapper> 